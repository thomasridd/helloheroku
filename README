Hello Java
==========

You should be able to clone and run this project if you have Java and Maven installed.

    mvn clean install
    java -jar target/*-jar-with-dependencies.jar
    // See startup information ..

In a separate terminal:

    curl http://localhost:8080/rest
    curl -X POST http://localhost:8080/rest

Enjoy playing!

David
